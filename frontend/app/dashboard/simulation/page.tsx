import { SimulationEngine } from "@/components/simulation/SimulationEngine";

export const metadata = {
  title: "Simulation - TeenVerse",
};

export default function SimulationPage() {
  return <SimulationEngine />;
}
